# AGENTS.md

## このリポジトリについて
このリポジトリは **Spec Kit による仕様駆動開発（Spec-Driven Development）** を前提としています。  
GitHub Copilot は、コードを書く前に **仕様（Spec）と計画（Plan）を最優先**で参照してください。

---

## 基本原則（最重要）
- 実装より先に **spec / plan / tasks の整合性を確認**すること
- 推測で実装しない。不明点は **spec の修正・追記を提案**すること
- 既存仕様と矛盾する実装は行わない

---

## 作業の進め方（必須フロー）
Copilot は、原則として以下の順序で行動してください。

1. **spec.md を読む**
   - 目的（Why）
   - 要求（What）
   - 制約・前提条件
2. **plan.md を読む**
   - 技術スタック
   - 構成・方針
   - 非機能要件
3. **tasks.md を確認**
   - 対象タスク
   - 依存関係
4. **そのタスク範囲のみ実装**
   - スコープ外の変更はしない

---

## 実装時のルール
- 変更対象は **tasks.md に記載された範囲のみ**
- 既存コードの意味を変える場合は、その理由を明示する
- 暗黙の仕様を新たに作らない
- リファクタ・最適化は明示的に求められた場合のみ行う

---

## コード品質・スタイル
- 可読性を最優先（短さより明確さ）
- 既存のコード規約・ディレクトリ構造を尊重
- 魔法のような抽象化や過度な最適化は避ける

---

## テスト・検証
- 仕様で要求されている場合はテストを追加
- 既存テストがある場合は必ず破壊しない
- テストが書けない場合は理由を説明する

---

## 仕様との不整合を見つけた場合
以下のいずれかを行うこと：

- spec / plan / tasks の修正案を提示する
- 実装を止め、確認事項として列挙する

---

## 禁止事項
- spec / plan / tasks を無視した実装
- 「一般的にはこうだから」という理由での仕様変更
- タスク外のファイルを勝手に変更すること

---

## 最終判断
このリポジトリにおける **唯一の正解は spec にある**。  
Copilot は「速く書く」より「仕様に忠実である」ことを優先してください。