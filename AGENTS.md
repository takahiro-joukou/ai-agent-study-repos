# AGENTS.md

---

## 基本原則（最重要）
- 実装より先に **spec / plan / tasks の整合性を確認**すること
- 推測で実装しない。不明点は **spec の修正・追記を提案**すること
- 既存仕様と矛盾する実装は行わない

---

## 作業の進め方（必須フロー）
Copilot は、原則として以下の順序で行動してください。

1. **spec.md を読む**
   - 目的（Why）
   - 要求（What）
   - 制約・前提条件
2. **plan.md を読む**
   - 技術スタック
   - 構成・方針
   - 非機能要件
3. **tasks.md を確認**
   - 対象タスク
   - 依存関係
4. **そのタスク範囲のみ実装**
   - スコープ外の変更はしない

---

## 実装時のルール
- 変更対象は **tasks.md に記載された範囲のみ**
- 既存コードの意味を変える場合は、その理由を明示する
- 暗黙の仕様を新たに作らない
- リファクタ・最適化は明示的に求められた場合のみ行う
- コメントは日本語で出力する
- JavaDoc は必ず生成する
- 既存のコード規約・ディレクトリ構造を尊重
- タスク外のファイルは勝手に変更しない

---

## 仕様との不整合を見つけた場合
以下のいずれかを行うこと：

- spec / plan / tasks の修正案を提示する
- 実装を止め、確認事項として列挙する
